# Инструкция по отправке проекта на GitHub

## ✅ Текущий статус

- ✅ Git инициализирован
- ✅ Файлы добавлены
- ✅ Первый коммит создан (34 файла)
- ✅ Ветка переименована в `main`
- ✅ Remote origin добавлен: `https://github.com/Nicelozion/finhelper.git`

## 🔐 Аутентификация на GitHub

Для отправки кода на GitHub нужна аутентификация. Выберите один из вариантов:

### Вариант 1: Personal Access Token (рекомендуется)

1. **Создайте токен на GitHub:**
   - Перейдите: https://github.com/settings/tokens
   - Нажмите "Generate new token" → "Generate new token (classic)"
   - Название: "FinHelper Project"
   - Срок действия: выберите нужный (например, 90 дней)
   - Права: отметьте `repo` (полный доступ к репозиториям)
   - Нажмите "Generate token"
   - **ВАЖНО**: Скопируйте токен сразу (он больше не будет показан!)

2. **Используйте токен для пуша:**
   ```bash
   cd /home/zxc/dev/finhelper
   git push -u origin main
   ```
   При запросе:
   - Username: `Nicelozion`
   - Password: **вставьте ваш Personal Access Token** (не пароль!)

### Вариант 2: Использовать токен в URL (для автоматизации)

```bash
cd /home/zxc/dev/finhelper
git remote set-url origin https://YOUR_TOKEN@github.com/Nicelozion/finhelper.git
git push -u origin main
```

**⚠️ Внимание**: Этот способ сохраняет токен в конфиге git (видно в `git remote -v`). Используйте только для теста.

### Вариант 3: Настроить SSH ключ

1. **Создайте SSH ключ:**
   ```bash
   ssh-keygen -t ed25519 -C "soloviov.mitia2017@yandex.ru"
   # Нажмите Enter для всех вопросов
   ```

2. **Добавьте ключ на GitHub:**
   ```bash
   cat ~/.ssh/id_ed25519.pub
   # Скопируйте вывод
   ```
   - Перейдите: https://github.com/settings/keys
   - Нажмите "New SSH key"
   - Вставьте ключ и сохраните

3. **Измените remote на SSH:**
   ```bash
   cd /home/zxc/dev/finhelper
   git remote set-url origin git@github.com:Nicelozion/finhelper.git
   git push -u origin main
   ```

## 🚀 После успешного пуша

Проверьте репозиторий:
https://github.com/Nicelozion/finhelper

## 📝 Текущая конфигурация

```bash
# Проверить remote:
git remote -v

# Проверить коммиты:
git log --oneline

# Статус:
git status
```
