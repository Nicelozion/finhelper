# Быстрый старт: Работа с типами пользователей

## ✅ Что реализовано

Backend **УЖЕ ПОЛНОСТЬЮ РЕАЛИЗОВАН** и правильно работает с 10 типами пользователей (`team053-1` до `team053-10`).

### Как это работает:

1. **Backend получает** параметр `user` (например, `team053-5`)
2. **Backend создает consent** с этим `client_id` в банковском API
3. **Банковское API** само определяет тип клиента и возвращает соответствующие данные
4. **Backend возвращает** данные клиенту

## 🚀 Запуск и тестирование

### Шаг 1: Запустите сервер

```powershell
# Windows PowerShell
cd c:\Project\finhelper\backend
go run .
```

```bash
# Linux/macOS
cd /path/to/finhelper/backend
go run .
```

**Ожидаемый вывод:**
```
Starting FinHelper Banking Aggregator
✓ Configuration loaded
✓ Team ID: team053
✓ Configured banks: 3
   - vbank: https://vbank.open.bankingapi.ru
   - abank: https://abank.open.bankingapi.ru
   - sbank: https://sbank.open.bankingapi.ru
✓ Server listening on :8080
```

### Шаг 2: Протестируйте разные типы пользователей

**Откройте НОВЫЙ терминал** и запустите тестовый скрипт:

```powershell
# Windows PowerShell
cd c:\Project\finhelper\backend
.\test_users.ps1
```

```bash
# Linux/macOS
cd /path/to/finhelper/backend
./test_users.sh
```

## 📝 Ручное тестирование

### Пример 1: Обычный сотрудник (team053-1)

```bash
# 1. Создаем consent
curl -X POST "http://localhost:8080/api/consents?bank=vbank&user=team053-1"

# Ответ:
{
  "ok": true,
  "bank": "vbank",
  "user": "team053-1",
  "consent_id": "consent-abc123",
  "message": "Consent created successfully"
}

# 2. Получаем счета
curl "http://localhost:8080/api/accounts?bank=vbank&user=team053-1"

# 3. Получаем продукты
curl "http://localhost:8080/api/products?bank=vbank&user=team053-1&type=DEPOSIT"
```

### Пример 2: VIP клиент (team053-2)

```bash
# 1. Создаем consent
curl -X POST "http://localhost:8080/api/consents?bank=vbank&user=team053-2"

# 2. Получаем счета (будут VIP счета с большими суммами)
curl "http://localhost:8080/api/accounts?bank=vbank&user=team053-2"

# 3. Получаем VIP депозиты (500k-2MP)
curl "http://localhost:8080/api/products?bank=vbank&user=team053-2&type=DEPOSIT"
```

### Пример 3: Студент (team053-5)

```bash
# 1. Создаем consent
curl -X POST "http://localhost:8080/api/consents?bank=vbank&user=team053-5"

# 2. Получаем счета (будут студенческие счета)
curl "http://localhost:8080/api/accounts?bank=vbank&user=team053-5"

# 3. Получаем студенческие продукты
curl "http://localhost:8080/api/products?bank=vbank&user=team053-5&type=DEPOSIT"
```

### Пример 4: VIP Premium (team053-10)

```bash
# 1. Создаем consent
curl -X POST "http://localhost:8080/api/consents?bank=vbank&user=team053-10"

# 2. Получаем счета (все типы счетов)
curl "http://localhost:8080/api/accounts?bank=vbank&user=team053-10"

# 3. Получаем ВСЕ доступные депозиты
curl "http://localhost:8080/api/products?bank=vbank&user=team053-10&type=DEPOSIT"

# 4. Получаем ВСЕ доступные кредиты
curl "http://localhost:8080/api/products?bank=vbank&user=team053-10&type=LOAN"

# 5. Получаем ВСЕ доступные карты
curl "http://localhost:8080/api/products?bank=vbank&user=team053-10&type=CARD"
```

## 🔍 Что вы увидите

### Различия между типами клиентов:

1. **team053-1 (employee)** - Базовые продукты
   - Стандартные счета
   - Обычные депозиты (до 500k)
   - Стандартные условия

2. **team053-2 (vip)** - VIP клиент
   - VIP счета
   - Депозиты 500k-2MP
   - Повышенные проценты

3. **team053-5 (student)** - Студент
   - Студенческие счета
   - Льготные условия
   - Специальные студенческие продукты

4. **team053-10 (vip premium)** - Все продукты
   - Все типы счетов
   - Все типы депозитов
   - Все типы кредитов
   - Все типы карт
   - Максимальные лимиты

## 📚 Документация

- **[README.md](README.md)** - Основная документация
- **[USER_TYPES.md](USER_TYPES.md)** - Подробное описание типов пользователей
- **[test_users.ps1](test_users.ps1)** - Автоматический тест (Windows)
- **[test_users.sh](test_users.sh)** - Автоматический тест (Linux/macOS)

## ❓ Частые вопросы

### Q: Где реализована логика типов клиентов?

**A:** В **банковском API**, НЕ в backend. Backend только передает `client_id`, а банк сам знает, какой это тип клиента и какие продукты ему доступны.

### Q: Как backend различает типы клиентов?

**A:** Backend НЕ различает. Он просто передает `user` параметр как `client_id` в банковское API. Вся бизнес-логика находится на стороне банка.

### Q: Почему разные пользователи получают разные данные?

**A:** Потому что банковское API возвращает разные данные в зависимости от `client_id`. Это правильное поведение согласно архитектуре Open Banking.

### Q: Нужно ли добавлять логику типов в backend?

**A:** **НЕТ!** Backend - это агрегатор. Он не должен знать о бизнес-правилах банков. Это нарушило бы принцип разделения ответственности.

## ✅ Проверка

Чтобы убедиться, что все работает:

1. ✅ Запустите сервер: `go run .`
2. ✅ Запустите тест: `.\test_users.ps1`
3. ✅ Проверьте, что для разных пользователей возвращаются разные данные
4. ✅ Убедитесь, что `team053-10` имеет доступ ко всем продуктам

## 🎯 Итог

**Backend работает ПРАВИЛЬНО!**

✅ Поддерживает все 10 типов пользователей
✅ Создает consent для каждого типа
✅ Передает `client_id` в банковское API
✅ Возвращает данные согласно типу клиента
✅ Документирован
✅ Протестирован

**Никаких изменений в код НЕ требуется!**
